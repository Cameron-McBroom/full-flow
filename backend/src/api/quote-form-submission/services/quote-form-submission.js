'use strict';

/**
 * quote-form-submission service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::quote-form-submission.quote-form-submission');
